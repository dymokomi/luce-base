# error: needs a `var` receiver

import c

struct Counter:
    var n: i64

    mutating func bump():
        self.n += 1

func f() -> i64:
    let counter = Counter(n = 0)
    counter.bump()
    return counter.n
