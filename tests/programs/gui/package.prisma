#prisma 4.0
def package "gui" {
    def native "inputs" {
        str[] sources = ["shims.c"]
        str[] pkg_config = ["sdl3"]
    }

    def dependency "luce-std" {
        str owner = "dymokomi"
        str version = "^0.1.0"
        str path = "../../../../luce-std"
    }
}
