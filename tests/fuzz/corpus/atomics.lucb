import thread
var counter: @u64 = 0
func worker(context: void*?):
    var i: u32 = 0
    while i < 100:
        counter += 1
        i += 1
pub func main(arguments: str[]) -> i32!:
    var h = try thread.spawn(worker, none)
    try h.join()
    print(f"{counter}")
    return 0
