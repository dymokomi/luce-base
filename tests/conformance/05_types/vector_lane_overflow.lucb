## §5.12: a lane computes as the scalar would, so a lane's integer overflow traps.
pub func main(arguments: str[]) -> i32:
    var big: i32[4] = [1, 2, 2147483647, 4]
    let more = big + 1
    print(f"{more[0]}")
    return 0
