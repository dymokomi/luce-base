# error: unary `-` is rejected on unsigned types
## §5.12: a lane follows the scalar rule, and an unsigned lane has no `-`.
pub func main(arguments: str[]) -> i32:
    let a: u32[4] = [1, 2, 3, 4]
    let c = -a
    return 0
