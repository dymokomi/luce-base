# error: makes a vector, and this array is not one
## §5.12: a vector is eight or sixteen bytes; `f32[3]` is twelve and has no lanes.
pub func main(arguments: str[]) -> i32:
    let c = f32[3](1.0)
    return 0
