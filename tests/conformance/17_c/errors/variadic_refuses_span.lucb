# error: 
## §17.2: a span is rejected in a variadic position.
import c
extern func printf(format: c.str, ...) -> i32

pub func main(arguments: str[]) -> i32:
    let bytes: u8[2] = [1, 2]
    let view: const u8[] = bytes
    discard(printf("%p", view))
    return 0
