# error: this name is already in scope
## §3.5: a local may not shadow a module-level binding either.
var count: i64

pub func main(arguments: str[]) -> i32:
    let count = 3
    return 0 if count == 3 else 1
