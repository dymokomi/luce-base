# error: 
## §20: `->` is not member access; `.` auto-dereferences.
struct P:
    var x: i64

pub func main(arguments: str[]) -> i32:
    var p = P(x = 1)
    let q = &p
    let n = q->x
    return 0
