# error: expected `u8[3]`, got `str`
## §4.4: a text literal is `str`; it does not initialise a byte array, `b"..."` does.
pub func main(arguments: str[]) -> i32:
    let bytes: u8[3] = "abc"
    return (i32)bytes[0]
