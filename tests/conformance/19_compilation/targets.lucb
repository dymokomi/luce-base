## §19.5, §19.6: `build --target` with no argument lists the six targets and the `asm`
## architectures the program covers.
export naked func spin():
    asm arm64:
        ret
    asm x86_64:
        ret

pub func main(arguments: str[]) -> i32:
    return 0
