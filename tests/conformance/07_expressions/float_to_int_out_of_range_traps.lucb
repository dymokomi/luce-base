## A trap the arithmetic and conversion rules require (§7.2, §7.3, §7.5, §7.6).
pub func main(arguments: str[]) -> i32:
    let huge: f64 = 1.0e20 * f64(arguments.length)
    let n = i32(huge)
    print(f"{n}")
    return 0
