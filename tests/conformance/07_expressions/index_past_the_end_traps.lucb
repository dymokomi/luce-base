## §7.7: only `&` admits the length as an index; reading `a[N]` traps.
pub func main(arguments: str[]) -> i32:
    var values: i32[4] = [1, 2, 3, 4]
    let n = values.length + arguments.length - 1
    print(f"{values[n]}")
    return 0
