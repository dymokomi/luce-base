# error: not assigned
## §10.1: a custom `init` assigns every field exactly once.
struct P:
    let a: i64
    let b: i64

    pub func init(a: i64):
        self.a = a

pub func main(arguments: str[]) -> i32:
    let p = P(1)
    return 0
