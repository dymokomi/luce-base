# error: not mutable
## §10.1: a mutable local updates `var` fields; a `let` local does not.
struct S:
    var x: i64

pub func main(arguments: str[]) -> i32:
    let s = S(x = 1)
    s.x = 2
    return 0
