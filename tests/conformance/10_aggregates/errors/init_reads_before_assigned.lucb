# error: before every field is assigned
## §10.1: `init` cannot read a field before every field is assigned.
struct P:
    let a: i64
    let b: i64

    pub func init(a: i64):
        self.a = a
        self.b = self.a * 2

pub func main(arguments: str[]) -> i32:
    let p = P(1)
    return 0
