# error: assigned twice
## §10.1: a custom `init` assigns every field exactly once.
struct P:
    let a: i64

    pub func init(a: i64):
        self.a = a
        self.a = a + 1

pub func main(arguments: str[]) -> i32:
    let p = P(1)
    return 0
