# error: 
## §10.3: the bit operators join an enum with an enum, not with an integer.
enum Access as u32:
    read = 1
    write = 2

pub func main(arguments: str[]) -> i32:
    let a = Access.read | 2
    return 0
