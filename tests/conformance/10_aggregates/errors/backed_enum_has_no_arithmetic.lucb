# error: 
## §10.3: `|`, `&`, `^`, `~` are defined on an integer-backed enum; `+` is not.
enum Access as u32:
    read = 1
    write = 2

pub func main(arguments: str[]) -> i32:
    let a = Access.read + Access.write
    return 0
