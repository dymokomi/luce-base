# error: 
## §12.2: the allocator named by `in` is a `var`, since its methods mutate it.
import memory

pub func main(arguments: str[]) -> i32!:
    var backing: u8[64]
    let arena = memory.FixedBuffer.over(backing)
    let p = try new i64 in arena
    return 0
