## §11.5: a trap in a `while` condition names the `while`, on every iteration, in every
## execution.
pub func main(arguments: str[]) -> i32:
    let items: i64[2] = [1, 2]
    var i: usize = 0
    while items[i] > 0:
        i += 1
    return 0
