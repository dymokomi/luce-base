# error: a deferred call must handle its failure with `catch`
pub let broken: ErrorCode = ErrorCode.package(1)
func might() -> !:
    return
pub func main(arguments: str[]) -> i32:
    defer might()
    return 0
