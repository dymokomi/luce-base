# error: `match` on this type needs a `_` arm
func name(n: i64) -> str:
    match n:
        1:
            return "one"
        2:
            return "two"
pub func main(arguments: str[]) -> i32:
    return 0
