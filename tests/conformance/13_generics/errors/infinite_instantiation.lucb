# error: 
## §13.1: an infinite chain of instantiations is rejected.
struct Pair[A, B]:
    var first: A
    var second: B

func grow[T](depth: T) -> i64:
    return grow(Pair[T, T](first = depth, second = depth))

pub func main(arguments: str[]) -> i32:
    return (i32)grow(1)
