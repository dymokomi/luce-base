#prisma 4.0
def package "luce-base" {
    str owner = "dymokomi"
    str version = "0.27.0"
    str kind = "tool"
    str language = "luce-base"
    str description = "The Luce Base compiler, written in Luce Base. This is the second stage of the bootstrap and the one that stays:"
    str readme = "README.md"
    str entry = "src/main.lucb"

    def dependency "luce-std" {
        str owner = "dymokomi"
        str version = "^0.1.0"
        str path = "../luce-std"
    }
}
